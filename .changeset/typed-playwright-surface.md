---
"@sightmap/sightkick": minor
---

Add a Playwright build target that emits a self-contained ES module and TypeScript declarations for typed tool calls and view waits, preserving the default IR output.
