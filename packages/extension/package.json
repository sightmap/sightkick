{
  "name": "@sightkick/extension",
  "version": "0.0.0",
  "private": true,
  "description": "MV3 extension that injects sightkick WebMCP tools into third-party sites (injector only; identical semantics to a direct install).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.21.5"
  }
}
