{
  "name": "@sightkick/runtime",
  "version": "0.0.0",
  "description": "Browser runtime that executes sightkick IR tools via the UI.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./executor": "./src/executor.ts"
  },
  "scripts": {
    "build": "node build.mjs",
    "demo": "node serve.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "eval": "node eval/run.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "happy-dom": "^15.11.7"
  }
}
