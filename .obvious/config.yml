# Repo policy for obvious agents (auto-detected from GitHub settings 2026-09-03).
defaultBranch: main
# Repo allows merge commits, squash, and rebase; squash is chosen for single-purpose setup PRs.
mergeMethod: squash
