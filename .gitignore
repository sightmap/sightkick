node_modules/
dist/
# bundled demo artifact
**/demo/*.bundle.js
*.tsbuildinfo
.DS_Store
.helix/
# local-only task herd
.yaks/
config.yaml
# generator scratch output during dev (but keep test goldens)
*.ir.json
!**/testdata/*.ir.json
**/.sightmap/.session

# release build outputs (goreleaser + npm packaging)
generator/dist/
npm-staging/
